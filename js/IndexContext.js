
define(["host"], function( Host ) {

    var host;

    function IndexContext(){
        host = new Host;
    };

    IndexContext.prototype.init = function() {
/*
        starter.init() ;
*/
    };

    return IndexContext;



});
